console.log("This is the question i'm responding to <%= @question.id %>");
<% if @answers.size > 0 %>
	<% @answers.each do |a| %>
		$("#pending_answer_table").append("<%= escape_javascript render(a) %>");
	<% end %>
<% end %>